const path = require('path');
const build_base_path = path.resolve('build-node', 'db');
const src_base_path = path.resolve('src-node', 'db');

module.exports = {
    'config': path.resolve('sequelize-config', 'config.json'),
    'options-path': 'sequelize-config',
    'migrations-path': path.resolve(build_base_path, 'migrations'),
    'models-path': path.resolve(build_base_path, 'models'),
    'seeders-path': path.resolve(build_base_path, 'seeders')
}