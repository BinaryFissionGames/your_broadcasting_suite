import * as assert from "assert";

describe('test', function () {
    describe('#test', function () {
        it("shouldn't fail", function () { // Simple test to make sure I have mocha set up correctly
            assert.strictEqual(1, 1);
        });
    });
});